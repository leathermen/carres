# Cars Reservation System - Rabbit MQ

A preconfigured image is used.

Requires the manual adjustment once launched (to create the app level user and virtual host).

The base admin pwd is configured via secrets.